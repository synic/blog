package view

templ ErrorView(title, message string) {
	@BaseLayout(title) {
		<div class="flex flex-row w-full">
			<div class="grow">
				<h2 class="mt-0 text-rose-500">{ title }</h2>
				<p class="mt-0">
					{ message }
				</p>
			</div>
		</div>
	}
}
